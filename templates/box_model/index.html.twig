{% extends 'base.html.twig' %}

{% block title %}
    Organizer: {{ boxModel.label }}
{% endblock %}

{% block body %}
<div class="container main-window">
    <div class="row mb-3 text-center">
        <div class="col">
            {% if boxModel.id is not null %}
                <h4>{{ boxModel.label }}</h4>
            {% else %}
                <h4>New Box Model</h4>
            {% endif %}
        </div>
    </div>
    {{ form(form) }}
</div>
{% endblock %}
